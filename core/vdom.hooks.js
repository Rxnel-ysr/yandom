"use strict"; import { value, valueComputed } from "../helper/helper.js"; import VDOM from "./vdom.js"; function onReady(cb, delay = 1000) { if (document.readyState === "loading") { document.addEventListener("DOMContentLoaded", () => { setTimeout(cb, delay); }); } else { setTimeout(cb, delay); } } class RootError extends Error { constructor(name) { super( `[hooks] "${name}" was called without an active root.\n` + `Hooks can only run while a Root is rendering. Start one first:\n\n` + `    const hooks = new Hooks(vdom);\n` + `    hooks.createRoot("#app").render(App);\n\n` + `If you are calling "${name}" outside of a component body ` + `(module scope, an event handler, a setTimeout callback, ...), ` + `move it inside the render function.`, ); this.name = "RootError"; } } class Root { constructor(hooksRuntime, target) { this.hooksRuntime = hooksRuntime; this.vdom = hooksRuntime.vdom; this.hooks = { next: null }; this.hookNode = null; this.vdomTree = null; this.target = this.vdom.getTarget(target); this.renderFn = null; } render(app) { this.renderFn = app; this.hooksRuntime.handler = () => this.rerender(); this.rerender(); return this; } use(any) { if ("prepare" in any) { any.prepare(this); } else { throw Error("Incompatible mod type."); } return this; } setRenderFn(fn) { this.renderFn = fn; } rerender() { requestAnimationFrame(() => { const runtime = this.hooksRuntime; if (typeof runtime.renderDebounce == "number") { clearTimeout(runtime.renderDebounce); runtime.renderDebounce = null; } runtime.renderDebounce = setTimeout(() => { try { runtime.currentComponent = this; runtime.resetContext(); const newVNode = this.renderFn(); if (!this.vdomTree) { this.vdomTree = this.vdom.render(newVNode, this.target); } else { this.vdomTree = this.vdom.update( this.target, this.vdomTree, newVNode, ); } } catch (error) { this.target.innerHTML = `<pre>${error.stack}</pre>`; console.error(error); } }, 33); onReady(() => this.vdom.executeJobs(), 300); }); } } class Hooks { constructor(vdom) { this.vdom = vdom; this.currentComponent = null; this.handler = null; this.disableRerender = false; this.renderDebounce = null; } requireRoot(name) { if (!this.currentComponent) throw new RootError(name); return this.currentComponent; } requireHookNode(name) { const root = this.requireRoot(name); if (!root.hookNode) root.hookNode = root.hooks; return root.hookNode; } nextNode(hookNode) { return (hookNode.next = hookNode.next || { next: null }); } resetContext() { const root = this.requireRoot("resetContext"); root.hookNode = root.hooks; } triggerRerender() { if (this.handler) this.handler(); } trailMaker(n = 1) { let head = { next: null }; let node = head; for (let i = 1; i <= n; i++) { node = node.next = { next: null }; } return [head, node]; } resets(n = 1) { let hookNode = this.requireHookNode("resets"); for (let i = 1; i <= n && hookNode.next; i++) { hookNode.value = undefined; hookNode = hookNode.next; } } allocate(n) { let start = this.requireHookNode("allocate"); let actual = n - 1; if (actual > -1) { let [head, tail] = this.trailMaker(actual); tail.next = start.next; start.next = head; } } overwrite(array, recompute = false) { let start = this.requireHookNode("overwrite"); array.map((e) => { if (recompute && typeof e?.recompute !== "undefined") { e.recompute = true; } start.value = e; start = start.next; }); } orphan(n) { let start = this.requireHookNode("orphan"); let end = start; for (let i = 1; i <= n; i++) { end = end.next = end.next; } start.next = undefined; start.next = end?.next || null; } getData(until) { const hookNode = this.requireHookNode("getData"); let n = 0; let data = []; let current = hookNode; while (n < until && current) { data.push(current?.value || undefined); current = current?.next; n++; } return data; } getCurrentHookNode() { return this.requireHookNode("getCurrentHookNode"); } countHooks(stringFn) { return ( stringFn.match( /(?<!\/\/[^\n]*)(useEffect\(|useState\(|useRef\(|useMemo\()/gm, ) || [] ).length; } comp( compFn, args = {}, options = { name: null, hook: null, remember: false, recompute: false, invalidAfter: undefined, }, ) { let name, counter = 0, result = { render: () => compFn(args), isComp: true, remember: value(options?.remember, false), recompute: value(options?.recompute, false), invalidAfter: value(options?.invalidAfter, 0), stringified: null, compHooks: null, }; counter = valueComputed(options?.hook, () => this.countHooks(compFn.toString()), ); name = valueComputed( options?.name, () => this.comp.toString() + JSON.stringify(options), ); result.stringified = name; result.compHooks = counter; return result; } destroy() { const root = this.requireRoot("destroy"); let hookNode = this.requireHookNode("destroy"); while (hookNode.next) { hookNode.value = undefined; hookNode = hookNode.next; } root.hookNode = hookNode; } useState(initial, handleInputEvent = false) { const root = this.requireRoot("useState"); let hookNode = this.requireHookNode("useState"); if (typeof hookNode.value === "undefined") { hookNode.value = initial; } const set = (val) => { if (handleInputEvent && val instanceof InputEvent) { val = val.target.value; } hookNode.value = typeof val == "function" ? val(hookNode?.value) : val; if (!this.disableRerender) { root.rerender(); } }; root.hookNode = this.nextNode(hookNode); return [hookNode?.value, set]; } bulkSetState(callback) { const root = this.requireRoot("bulkSetState"); this.disableRerender = true; try { callback(); } finally { this.disableRerender = false; } root.rerender(); } useRef(initial) { const root = this.requireRoot("useRef"); let hookNode = this.requireHookNode("useRef"); if (typeof hookNode.value === "undefined") { hookNode.value = { current: initial }; } root.hookNode = this.nextNode(hookNode); return hookNode?.value; } useEffect(effect, deps = null) { const root = this.requireRoot("useEffect"); let hookNode = this.requireHookNode("useEffect"); const hasNoDeps = !deps; const oldHook = hookNode?.value; const hasChangedDeps = typeof oldHook !== "undefined" ? oldHook?.recompute || !deps.every((dep, j) => Object.is(dep, oldHook.deps[j])) : true; if (hasNoDeps || hasChangedDeps) { if (oldHook?.cleanup) { queueMicrotask(() => { oldHook.cleanup?.(); }); } queueMicrotask(() => { const cleanup = effect(); hookNode.value = { deps, cleanup, recompute: false }; }); } else { hookNode.value = oldHook; } root.hookNode = this.nextNode(hookNode); } useMemo(compute, deps) { const root = this.requireRoot("useMemo"); let hookNode = this.requireHookNode("useMemo"); const prev = hookNode?.value; const hasNoDeps = !deps; const hasChanged = prev ? !deps.every((d, j) => Object.is(d, prev.deps[j])) : true; if (hasNoDeps || hasChanged) { const value = compute(); hookNode.value = { value, deps }; root.hookNode = hookNode.next = hookNode.next || { next: null, }; return value; } root.hookNode = this.nextNode(hookNode); return prev.value; } createRoot(root) { return this.currentComponent = new Root(this, root); } hmr(config, app, enabled = false) { if (enabled) { const wsPort = config?.ws?.port || 4040, wsHost = config?.ws?.host || location.hostname, main = config?.main || "./src/app.js"; const socket = new WebSocket(`ws://${wsHost}:${wsPort}`); socket.addEventListener("message", async ({ data }) => { const msg = JSON.parse(data); if (msg.type === "reload") { try { console.log(`[HMR]: ${msg.path}`); const mod = await import(`${main}?t=` + msg.timestamp); if (mod.default) { app.setRenderFn(mod.default); app.rerender(); } } catch (error) { console.log(error); } } }); } } } export { Hooks, Root, RootError }; export default Hooks;
