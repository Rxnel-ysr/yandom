"use strict"; import { value, valueComputed } from "../helper/helper.js"; import { RenderVDOM, executeJobs, getTarget } from "./vdom.js"; let currentComponent = null, handler = null, disableRerender = false, renderDebounce = null; const nextNode = (hookNode) => { return (hookNode.next = hookNode.next || { next: null }); }; const resetContext = () => { currentComponent.hookNode = currentComponent.hooks; }; const triggerRerender = () => { if (handler) handler(); }; const trailMaker = (n = 1) => { let head = { next: null }; let node = head; for (let i = 1; i <= n; i++) { node = node.next = { next: null }; } return [head, node]; }; const resets = (n = 1) => { if (!currentComponent) return; let hookNode = currentComponent.hookNode; if (!hookNode) return; for (let i = 1; i <= n && hookNode.next; i++) { hookNode.value = undefined; hookNode = hookNode.next; } }; const allocate = (n) => { let start = currentComponent.hookNode; let actual = n - 1; if (actual > -1) { let [head, tail] = trailMaker(actual); tail.next = start.next; start.next = head; } }; const overwrite = (array, recompute = false) => { let start = currentComponent.hookNode; array.map((e) => { if (recompute && typeof e?.recompute !== "undefined") { e.recompute = true; } start.value = e; start = start.next; }); }; const orphan = (n) => { let start = currentComponent.hookNode; let end = start; for (let i = 1; i <= n; i++) { end = end.next = end.next; } start.next = undefined; start.next = end?.next || null; }; const getData = (until) => { if (!currentComponent) return; const hookNode = currentComponent.hookNode; if (!hookNode) return; let n = 0; let data = []; let current = hookNode; while (n < until && current) { data.push(current?.value || undefined); current = current?.next; n++; } return data; }; const getCurrentHookNode = () => { if (!currentComponent) return; return currentComponent.hookNode; }; function countHooks(stringFn) { return ( stringFn.match( /(?<!\/\/[^\n]*)(useEffect\(|useState\(|useRef\(|useMemo\()/gm, ) || [] ).length; } const comp = ( compFn, args = {}, options = { name: null, hook: null, remember: false, recompute: false, invalidAfter: undefined, }, ) => { let name, counter = 0, result = { render: () => compFn(args), isComp: true, remember: value(options?.remember, false), recompute: value(options?.recompute, false), invalidAfter: value(options?.invalidAfter, 0), stringified: null, compHooks: null, }; counter = valueComputed(options?.hook, () => countHooks(compFn.toString())); name = valueComputed( options?.name, () => comp.toString() + JSON.stringify(options), ); result.stringified = name; result.compHooks = counter; return result; }; const destroy = () => { if (!currentComponent) return; let hookNode = currentComponent.hookNode; if (!hookNode) return; while (hookNode.next) { hookNode.value = undefined; hookNode = hookNode.next; } currentComponent.hookNode = hookNode; }; const useState = (initial, handleInputEvent = false) => { let hookNode = currentComponent?.hookNode; if (typeof hookNode?.value === "undefined") { hookNode.value = initial; } const set = (val) => { if (handleInputEvent && val instanceof InputEvent) { val = val.target.value; } hookNode.value = typeof val == "function" ? val(hookNode?.value) : val; if (!disableRerender) { currentComponent.rerender(); } }; currentComponent.hookNode = nextNode(hookNode); return [hookNode?.value, set]; }; const bulkSetState = (callback) => { disableRerender = true; callback(); disableRerender = false; currentComponent.rerender(); }; const useRef = (initial) => { let hookNode = currentComponent.hookNode; if (typeof hookNode?.value === "undefined") { hookNode.value = { current: initial }; } currentComponent.hookNode = nextNode(hookNode); return hookNode?.value; }; const useEffect = (effect, deps = null) => { let hookNode = currentComponent.hookNode; const hasNoDeps = !deps; const oldHook = hookNode?.value; const hasChangedDeps = typeof oldHook !== "undefined" ? oldHook?.recompute || !deps.every((dep, j) => Object.is(dep, oldHook.deps[j])) : true; if (hasNoDeps || hasChangedDeps) { if (oldHook?.cleanup) { queueMicrotask(() => { oldHook.cleanup?.(); }); } queueMicrotask(() => { const cleanup = effect(); hookNode.value = { deps, cleanup, recompute: false }; }); } else { hookNode.value = oldHook; } currentComponent.hookNode = nextNode(hookNode); }; const useMemo = (compute, deps) => { let hookNode = currentComponent.hookNode; const prev = hookNode?.value; const hasNoDeps = !deps; const hasChanged = prev ? !deps.every((d, j) => Object.is(d, prev.deps[j])) : true; if (hasNoDeps || hasChanged) { const value = compute(); hookNode.value = { value, deps }; currentComponent.hookNode = hookNode.next = hookNode.next || { next: null, }; return value; } currentComponent.hookNode = nextNode(hookNode); return prev.value; }; function onReady(cb, delay = 1000) { if (document.readyState === "loading") { document.addEventListener("DOMContentLoaded", () => { setTimeout(cb, delay); }); } else { setTimeout(cb, delay); } } function hmr(config, app, enabled = false) { if (enabled) { const wsPort = config?.ws?.port || 4040, wsHost = config?.ws?.host || location.hostname, main = config?.main || "./src/app.js"; const socket = new WebSocket(`ws://${wsHost}:${wsPort}`); socket.addEventListener("message", async ({ data }) => { const msg = JSON.parse(data); if (msg.type === "reload") { try { console.log(`[HMR]: ${msg.path}`); const mod = await import(`${main}?t=` + msg.timestamp); if (mod.default) { app.setRenderFn(mod.default); app.rerender(); } } catch (error) { console.log(error); } } }); } } function createRoot(root) { const comp = { render(app) { comp.renderFn = app; currentComponent = comp; handler = comp.rerender; comp.rerender(); return comp; }, use(any) { if ("prepare" in any) { any.prepare(this); } else { throw Error("Incompatible mod type."); } return comp; }, hooks: { next: null }, hookNode: null, vdom: null, target: getTarget(root), renderFn: null, setRenderFn(fn) { comp.renderFn = fn; }, rerender() { requestAnimationFrame(() => { if (typeof renderDebounce == "number") { clearTimeout(renderDebounce); renderDebounce = null; } renderDebounce = setTimeout(() => { try { resetContext(); const newVNode = comp.renderFn(); if (!comp.vdom) { comp.vdom = RenderVDOM.render(newVNode, comp.target); } else { comp.vdom = RenderVDOM.update(comp.target, comp.vdom, newVNode); } } catch (error) { comp.target.innerHTML = `<pre>${error.stack}</pre>`; console.error(error); } }, 33); onReady(executeJobs, 300); }); }, }; return comp; } export { resetContext, useState, useEffect, useMemo, useRef, createRoot, resets, getCurrentHookNode, destroy, comp, allocate, orphan, overwrite, triggerRerender, getData, bulkSetState, hmr, };
