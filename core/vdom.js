"use strict"; import { Hooks } from "./vdom.hooks.js"; import Memory from "./memory.js"; const memoryPrefix = "ComponentState_"; class VDOM { constructor(memory, hooks = null) { this.jobs = []; this.memory = memory; this.memoryPrefix = memoryPrefix; this._keys = {}; this.customVDom = {}; this.hooks = hooks; this.html = this._createHtmlProxy(); this.RenderVDOM = { createVNode: this.createVNode.bind(this), render: this.render.bind(this), update: this.update.bind(this), }; } setHooks(hooks) { this.hooks = hooks; } static isVNode(v) { return ( typeof v == "object" && v?.isComp === false && typeof v?.props == "object" && typeof v?.tag == "string" ); } static isVNodeComponent(v) { return ( typeof v == "object" && v?.isComp === true && typeof v?.compHooks == "number" && typeof v?.stringified == "string" && typeof v?.remember == "boolean" && typeof v?.recompute == "boolean" && typeof v?.invalidAfter == "number" && typeof v?.render == "function" ); } getKey(vnode) { return vnode?.props?.key ?? null; } hasKey(vnode) { return vnode && typeof vnode.props?.key !== "undefined"; } setKey(key, vnode) { this._keys[key] = vnode.el; } pushJob(fn) { this.jobs.push(fn); } executeJobs() { for (const job of this.jobs) job(); this.jobs.length = 0; } filterFalsy(c) { return c !== false && c !== null && c !== undefined; } flattenChildren(children) { return children.flat(10).filter(this.filterFalsy); } createVNode(tag, props = {}, ...children) { let flatten = this.flattenChildren(children); let keyed = false; for (let i = 0; i < flatten.length; i++) { if (flatten[i]?.props?.key !== undefined) { keyed = true; break; } } if (keyed) { props.keyed = true; } return { tag, stringifiedProps: JSON.stringify(props), props, children: flatten.map((n) => this.wrapPrimitive(n)), isComp: false, }; } wrapPrimitive(node) { if (typeof node == "function") { node = node(); } if (["string", "number"].includes(typeof node)) { const text = String(node); return { tag: "#text", text: text, children: [text], props: {}, el: document.createTextNode(text), isComp: false, }; } return node; } updateRef(ref, value) { if (!ref) return; try { if (typeof ref === "function") { ref(value); } else if (ref && typeof ref === "object" && "current" in ref) { ref.current = value; } } catch (e) { console.error("Error updating ref:", e); } } cleanupVNode(node) { if (!node || typeof node !== "object") return; const el = node.el; if (el && node.props) { for (const key in node.props) { const value = node.props[key]; if (key.startsWith("on") && typeof value === "function") { const event = key.slice(2).toLowerCase(); el.removeEventListener(event, value); } } } if (typeof node.props?.useCleanup === "function") { try { node.props.useCleanup(node.el); } catch (e) { } } const ref = node.props?.ref; if (ref && el?.isConnected === false) { this.updateRef(ref, null); } if (Array.isArray(node.children)) { for (const child of node.children) this.cleanupVNode(child); } node.children = null; node.props = null; } updateProps(el, oldProps, newProps) { const allProps = { ...oldProps, ...newProps }; for (const key in allProps) { const oldValue = oldProps[key]; const newValue = newProps[key]; if (key === "keyed") continue; if ( key === "useCleanup" && (typeof oldValue === "function" || typeof newValue == "function") ) { continue; } if (key === "ref") { if (oldValue !== newValue) { if (oldValue) { this.updateRef(oldValue, null); } if (newValue && el) { this.updateRef(newValue, el); } } continue; } if (newValue === undefined) { if (key === "class") { el.removeAttribute("class"); } else if (key === "style") { el.style.cssText = ""; } else if (key.startsWith("on") && typeof oldValue === "function") { el.removeEventListener(key.slice(2).toLowerCase(), oldValue); } else { el.removeAttribute(key); } } else if (oldValue !== newValue) { if (key === "class") { el.setAttribute( "class", Array.isArray(newValue) ? newValue.filter(Boolean).join(" ") : newValue, ); } else if (key === "style") { if (typeof newValue === "string") { el.style.cssText = newValue; } else { el.style.cssText = ""; Object.assign(el.style, newValue); } } else if (key.startsWith("on") && typeof newValue === "function") { if (oldValue) el.removeEventListener(key.slice(2).toLowerCase(), oldValue); el.addEventListener(key.slice(2).toLowerCase(), newValue); } else { el.setAttribute(key, newValue); } } } return newProps; } renderVNode(vnode, parentIsSvg = false) { let work; if (vnode.isComp) { work = vnode.vdom = vnode.render(); } else { work = vnode; } if (work.tag == "#text") { return work.el; } let isSvg = work.tag == "svg" || parentIsSvg; if (work.tag === "#fragment") { const start = document.createComment("fragment-start"); const end = document.createComment("fragment-end"); const frag = document.createDocumentFragment(); work.el = start; work._end = end; frag.appendChild(start); for (let child of work.children || []) { const el = this.renderVNode(child); if (el) frag.appendChild(el); } frag.appendChild(end); return frag; } const el = isSvg ? document.createElementNS("http://www.w3.org/2000/svg", work.tag) : document.createElement(work.tag); const ref = work.props?.ref; if (ref && el) { this.updateRef(ref, el); } if (work?.props) { for (const [key, value] of Object.entries(work.props)) { if (key === "useCleanup" && typeof value === "function") continue; if (key === "ref" || key === "keyed") continue; if (key === "key") this.setKey(value, work); if (key === "class") { if (isSvg) { el.setAttribute( "class", Array.isArray(value) ? value.filter(Boolean).join(" ") : value, ); } else { el.className = Array.isArray(value) ? value.filter(Boolean).join(" ") : value; } } else if (key === "style") { if (typeof value === "string") { el.style.cssText = value; } else { Object.assign(el.style, value); } } else if (key.startsWith("on") && typeof value === "function") { el.addEventListener(key.slice(2).toLowerCase(), value); } else { el.setAttribute(key, value); } } if (work.props?.shadow) { const shadow = el.attachShadow({ mode: work.props.shadow === true ? "open" : work.props.shadow, }); el._shadow = shadow; } } const children = Array.isArray(work.children) ? work.children : [work.children]; for (let child of children) { if (child === null || child === undefined) continue; el.appendChild(this.renderVNode(child, isSvg)); } work.el = el; return el; } patchChildrenWithKeys(parent, oldChildren, newChildren) { const oldKeyMap = new Map(); oldChildren.forEach((vnode) => oldKeyMap.set(vnode.props.key, vnode)); const newKeySet = new Set(); const updatedChildren = []; newChildren.forEach((newVNode, i) => { const key = newVNode.props.key; newKeySet.add(key); const oldVNode = oldKeyMap.get(key); if (oldVNode) { if (oldVNode.stringifiedProps != newVNode.stringifiedProps) { requestAnimationFrame(() => { this.updateProps(oldVNode.el, oldVNode.props, newVNode.props); }); } const oldChildren = oldVNode.children || []; const newChildren = newVNode.children || []; const max = Math.max(oldChildren.length, newChildren.length); for (let i = 0; i < max; i++) { this.patch(oldVNode.el, oldChildren[i], newChildren[i]); } newVNode.el = oldVNode.el; updatedChildren.push(newVNode); } else { const el = this.renderVNode(newVNode); newVNode.el = el; parent.insertBefore(el, parent.children[i] || null); updatedChildren.push(newVNode); } }); oldChildren.forEach((oldVNode) => { if (!newKeySet.has(oldVNode.props.key)) { this.cleanupVNode(oldVNode); parent.removeChild(oldVNode.el); } }); updatedChildren.forEach((vnode, i) => { const current = parent.children[i]; if (vnode.el !== current) { parent.insertBefore(vnode.el, current); } }); return updatedChildren; } handleComponentState(old, replacement) { let oldHookCount = old.compHooks, replacementHookCount = replacement.compHooks; if (oldHookCount === 0 && replacementHookCount > 0) { return this.handleComponentApplyState(replacement); } else if (replacementHookCount === 0 && oldHookCount > 0) { return this.handleComponentRetrieval(old); } else if (replacementHookCount === 0 && oldHookCount === 0) { return; } let current = this.hooks.getCurrentHookNode(); let store = new Array(oldHookCount); let storedMemory = []; let prev = null; if ( replacement.remember && this.memory.remembered(this.memoryPrefix + replacement.stringified) ) { storedMemory = this.memory.recall( this.memoryPrefix + replacement.stringified, ); } for (let i = 0; i < Math.max(oldHookCount, replacementHookCount); i++) { if (i > oldHookCount) { let newNode = { value: undefined, next: current?.next }; if (!current) { prev.next = current = newNode; } else { current.next = newNode; prev = current; current = newNode; } } else { if (old.remember) { store[i] = current.value; } } if (current.value?.cleanup) { try { current.value.cleanup(); } catch (error) { } } current.value = undefined; if (replacement.remember) { current.value = storedMemory[i]; if ( replacement.recompute && typeof current.value?.recompute !== "undefined" ) { current.value.recompute = true; } } prev = current; current = current.next; } if (oldHookCount > replacementHookCount) { this.hooks.orphan(old.compHooks - replacement.compHooks); } if (old.remember) { this.memory.memorize( this.memoryPrefix + old.stringified, store, old.invalidAfter, ); } } handleComponentRetrieval(component) { let data = new Array(component.compHooks); let current = this.hooks.getCurrentHookNode(); for (let i = 0; i < component.compHooks; i++) { if (component.remember) { data[i] = current.value; } if (current.value?.cleanup) { try { current.value.cleanup(); } catch (error) { } } current.value = undefined; current = current.next; } this.hooks.orphan(component.compHooks - 1); if (component.remember) { this.memory.memorize( this.memoryPrefix + component.stringified, data, component.invalidAfter, ); } } handleComponentApplyState(component) { this.hooks.allocate(component.compHooks - 1); if ( component.remember && this.memory.remembered(this.memoryPrefix + component.stringified) ) { this.hooks.overwrite( this.memory.recall(this.memoryPrefix + component.stringified), component.recompute, ); } } handleComponent(parent, old, newOne) { if (VDOM.isVNodeComponent(old) && VDOM.isVNodeComponent(newOne)) { if (old.stringified !== newOne.stringified) { this.handleComponentState(old, newOne); } newOne.vdom = this.patch(parent, old.vdom, newOne.render(), true); return newOne; } else if (VDOM.isVNodeComponent(old) && !VDOM.isVNodeComponent(newOne)) { this.handleComponentRetrieval(old); return this.patch(parent, old.vdom, newOne, true); } else if (!VDOM.isVNodeComponent(old) && VDOM.isVNodeComponent(newOne)) { this.handleComponentApplyState(newOne); newOne.vdom = this.patch(parent, old, newOne.render(), true); return newOne; } else { console.error("Impossible", old, newOne); return null; } } lastNode(vnode) { if (!vnode) return null; return vnode.tag === "#fragment" ? (vnode._end ?? vnode.el) : vnode.el; } insertAfter(parent, el, after) { if (after) after.after(el); else parent.appendChild(el); } removeFragment(frag) { let node = frag.el; const end = this.lastNode(frag); if (!node || !end) return; while (node) { const next = node === end ? null : node.nextSibling; node.remove(); node = next; } } replaceFragment(frag, newEl) { const end = this.lastNode(frag); if (!end) return; end.after(newEl); this.removeFragment(frag); } patch(parent, oldNode, newNode, skip = false, after = null) { if (oldNode == null && newNode == null) return null; if ( !skip && (VDOM.isVNodeComponent(oldNode) || VDOM.isVNodeComponent(newNode)) ) { return this.handleComponent(parent, oldNode, newNode, after); } if (newNode == null) { this.cleanupVNode(oldNode); if (oldNode.tag === "#fragment") this.removeFragment(oldNode); else oldNode.el?.remove(); return null; } if (newNode.tag === "#text") { if (oldNode?.tag === "#text") { const oldText = oldNode.children?.[0]; const newText = newNode.children?.[0]; if (oldText !== newText && oldNode.el) { oldNode.el.nodeValue = newText; } newNode.el = oldNode.el; return newNode; } const newEl = this.renderVNode(newNode); if (oldNode?.tag === "#fragment") { this.cleanupVNode(oldNode); this.replaceFragment(oldNode, newEl); } else if (oldNode?.el) { parent.replaceChild(newEl, oldNode.el); } else { this.insertAfter(parent, newEl, after); } newNode.el = newEl; return newNode; } if (oldNode == null) { const el = this.renderVNode(newNode); this.insertAfter(parent, el, after); if (newNode.tag !== "#fragment") newNode.el = el; return newNode; } if (oldNode.tag === "#fragment" && newNode.tag !== "#fragment") { this.cleanupVNode(oldNode); const el = this.renderVNode(newNode); this.replaceFragment(oldNode, el); newNode.el = el; return newNode; } if (oldNode.tag === "#fragment" && newNode.tag === "#fragment") { this.patchFragmentChild(parent, oldNode, newNode); newNode.el = oldNode.el; newNode._end = oldNode._end; return newNode; } if (oldNode.tag !== newNode.tag) { this.cleanupVNode(oldNode); const el = this.renderVNode(newNode); parent.replaceChild(el, oldNode.el); if (newNode.tag !== "#fragment") newNode.el = el; return newNode; } if (newNode.tag === "svg") { this.cleanupVNode(oldNode); const el = this.renderVNode(newNode, true); parent.replaceChild(el, oldNode.el); newNode.el = el; return newNode; } if (oldNode.stringifiedProps !== newNode.stringifiedProps) { requestAnimationFrame(() => { this.updateProps(oldNode.el, oldNode.props || {}, newNode.props || {}); }); } if (newNode.tag === "input" && oldNode.el?.value !== newNode.props?.value) { oldNode.el.value = newNode.props.value; } const oldChildren = oldNode.children || []; const newChildren = newNode.children || []; if (oldNode.props?.keyed && newNode.props?.keyed) { this.patchChildrenWithKeys(oldNode.el, oldChildren, newChildren); } else { const max = Math.max(oldChildren.length, newChildren.length); for (let i = 0; i < max; i++) { this.patch(oldNode.el, oldChildren[i], newChildren[i]); } } newNode.el = oldNode.el; return newNode; } patchFragmentChild(parent, oldFragment, newFragment) { const oldChildren = oldFragment.children; const newChildren = newFragment.children; const max = Math.max(oldChildren.length, newChildren.length); let cursor = oldFragment.el; for (let i = 0; i < max; i++) { const result = this.patch( parent, oldChildren[i], newChildren[i], false, cursor, ); if (result) cursor = this.lastNode(result); } return newFragment; } render(vnode, container) { container = typeof container === "string" ? this.getTarget(container) : container; container.innerHTML = ""; const node = typeof vnode === "string" ? vnode : this.createVNode(vnode.tag, vnode.props, vnode.children); return this.patch(container, null, node); } update(container, oldNode, newNode) { return this.patch(container, oldNode, newNode); } getTarget(selector, scope = document) { if (selector instanceof Node || selector instanceof Document) { return scope; } const target = scope.querySelector(selector); if (!target) throw new Error(`Target "${scope}" not found`); return target; } registerVdom(tag, resolver) { this.customVDom[tag] = resolver; } vnode(tag, props, ...children) { let propType = typeof props; if (propType === "string" || propType === "number") { return this.createVNode(tag, {}, props, children); } else if (Array.isArray(props)) { return this.createVNode(tag, {}, props, children); } else if (children.length == 0 && props?.tag) { return this.createVNode(tag, {}, props); } else { return this.createVNode(tag, props, children); } } renderTag(tag, props = {}, ...children) { return this.renderVNode(this.createVNode(tag, props, children)); } _createHtmlProxy() { const self = this; const actions = { mount: (el, selector, scope = document) => self.getTarget(selector, scope).replaceChildren(el), push: (el, selector, scope = document) => self.getTarget(selector, scope).appendChild(el), mountShadow: (el, selector, scope = document) => { const target = self.getTarget(selector, scope); if (!target._shadow) { target._shadow = target.attachShadow({ mode: "open" }); } target._shadow.replaceChildren(el); return target._shadow; }, element: (tag, props = {}, ...children) => self.createVNode(tag, props, children), vdom: self.RenderVDOM, _: (tag, props = {}, ...children) => self.renderTag(tag, props, ...children), $: (...children) => ({ tag: "#fragment", children: self .flattenChildren(children) .map((n) => self.wrapPrimitive(n)), isComp: false, }), }; return new Proxy(actions, { get: (target, tag) => { return ( target[tag] || self.customVDom[tag] || ((props = {}, ...children) => self.vnode(tag, props, ...children)) ); }, }); } } export default VDOM;
